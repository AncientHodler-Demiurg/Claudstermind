# Conventions — <ProjectName>

> Project-specific norms that *override or extend* the cluster-wide conventions in [`../../meta/shared-conventions.md`](../../meta/shared-conventions.md). If there aren't any project-specific overrides, this file just says so — the cluster conventions still apply.

## Overrides

<Cases where this project does something different from the cluster default. Each override states the rule + the reason.>

- 

## Extensions

<Rules that are project-specific and don't exist cluster-wide. E.g. "In this repo, migration numbers must be zero-padded to 3 digits.">

- 

## Nothing to override

If this project follows cluster conventions verbatim, say so here and delete the sections above:

> No project-specific overrides. See [`../../meta/shared-conventions.md`](../../meta/shared-conventions.md) for all applicable rules.
