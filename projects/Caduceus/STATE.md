# State — Caduceus

> Current-state snapshot. Updated at every session close. Authoritative for "what's the current version / what's in flight / what's outstanding".

- **Version at close:** Phase 0 restructure landed 2026-07-03. Tier structure reshuffled: **Arweave is now Tier I MVP (Phases 1–4)**; Bitcoin is Tier II Gateway (Phase 5) alongside Ethereum (Phase 6). Roadmap grew from 10 phases to 11. Last commit on `main`: `28b51f0` (`docs+infra: foreign-chain nodes are hub-managed…`). The tier-restructure edits (`web/index.html`, all `docs/*.md`, new `docs/modules/ouronet-arweave/DESIGN.md`, README) are staged to commit as the next push.
- **Open plan:** [`plans/PHASE_1.md`](../../../Caduceus/plans/PHASE_1.md) is the *old* Bitcoin-Phase-1 plan and now describes what will run when Bitcoin's turn comes at Phase 5. The Bitcoin scaffold from commit `a2ffc8f` (packages/{types, common, pact-client, btc-sniffer, btc-releaser} + e2e/) stays in the tree as Phase-5 pre-work. A new Arweave equivalent (`packages/ar-sniffer`, `packages/ar-releaser`) is not yet scaffolded — that's the actual next-code deliverable.
- **Last session (2026-07-03, Tier restructure):** User decision — bridged AR anchors sSTOA liquidity via an 80/20 sSTOA/DPTF-AR weighted pool at Phase 4 launch; that's the strategic reason Arweave outranks Bitcoin. Bridge only the native AR token (not AO/ARIO/PI — market cap too small to justify ops cost on the other three). Direct DPTF-AR.arweave mint (no Stoic Fungible wrap layer). Passive AO yield on custody AR is a real accounting question; Phase 1 policy default is "sweep to operator treasury address" (governable setting). Wrote `docs/modules/ouronet-arweave/DESIGN.md` (~340 lines, mirrors the Bitcoin one's shape but for Arweave — native tag binding, RSA-4096 custody, sniffer via direct node RPC, releaser as single-tx account-balance transfer, passive-AO-yield section). Cascaded updates through ROADMAP.md (rewrote from 10 to 11 phases), HANDOFF.md (tier table + DPTF list + node posture + glossary), ARCHITECTURE.md, README.md, HOSTING.md, TOOLKIT.md, LOGO.md, and the Bitcoin DESIGN.md's status header. Modules section on the landing page reshuffled (Arweave card added Tier I; Bitcoin moved to Tier II next to Ethereum; Modules 4–14 renumbered). Deferred: SVG mark redraw (still shows Bitcoin in the upper coil).
- **Known outstanding:**
  - **SVG caduceus mark still shows Bitcoin in the upper coil slot.** The tier structure is authoritative in text but the art hasn't caught up. Separate pass — bigger job.
  - **Arweave TS scaffold not yet written.** Bitcoin scaffold exists (`packages/btc-*`); Arweave equivalents (`packages/ar-sniffer`, `packages/ar-releaser`) need to be built once Phase 1 for Arweave gets triggered.
  - HSM choice still not locked.
  - USD oracle for the $50-min check unspecified.
  - Live `PactClient` not implemented — the `'live'` mode in `createPactClient()` throws.
  - Operator-side Pact code (`caduceus`, `bridge-ledger`, `dptf-ar.arweave` modules) is the operator's responsibility.
  - AR-yield sweep policy encoded as a governable setting (Phase 1 decision, MVP recommendation is sweep-to-operator-treasury).
- **Drift notes:** repo working tree has uncommitted tier-restructure edits (~10 files touched, ~500 lines net including new Arweave DESIGN.md). Live VPS clone still on `3fe455d` (Phase-0 landing page pre-DEV-warning); Phase 1 landing-page redraw pending SVG mark update — no VPS pull needed yet.
