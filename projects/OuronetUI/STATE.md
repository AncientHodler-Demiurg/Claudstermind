# State — OuronetUI

- **Version at close:** `0.29.8` (from `src/constants/version.ts`, commit `f37597e` on `dev`)
- **Open plan:** [`docs/EXTRACT_OURONET_CORE_PLAN.md`](../../../OuronetUI/docs/EXTRACT_OURONET_CORE_PLAN.md) — Phases -1.1, -1.3, -1.2, 1, 2a, 2b (+refinements), 2c, 3a, 3b.1, 3b.2, 3b cleanup, **4** complete. **Phase 5 next** — publish core to GitHub Packages + drop the `file:../OuronetCore` link.
- **Companion repo state:** `D:/_Claude/OuronetCore/` at commit `fd6cea8` on `main`, version `0.10.0`. 193 tests pass (was 162 before Phase 4 — 31 new encryption tests moved over). Both GitHub CIs green. Consumed via `file:../OuronetCore`.
- **Last session (2026-04-22):** Phase 4 shipped.
  - **Core v0.10.0** adds two entry points:
    - `@stoachain/ouronet-core/crypto` — migrated wholesale from OuronetUI's `src/lib/encryptor.ts` + `src/lib/encryptorV2.ts`. Both V1 (PBKDF2-SHA256 10k) and V2 (PBKDF2-SHA512 600k) primitives. `smartEncrypt` now PURE — takes `schemaVersion: string | null` as an argument instead of reading localStorage. Works in browser + Node identically.
    - `@stoachain/ouronet-core/codex` — portable Codex shape: `PlaintextCodex<KS, OA, PK, AB, UI>` generic (consumers plug their own wallet/account/keypair types via params), `CodexExportV1_2` with byte-for-byte-preserved `"1.2"` backup format, `buildCodexExport`/`serializeCodex`/`deserializeCodex`, `migrateSeedType` + `SeedType`/`RawSeedType` (extracted from WalletStorage).
    - 31 encryption tests moved from UI into core `tests/encryption.test.ts`. Covers V1/V2 round-trips, wrong-password, envelope shape, smartDecrypt mixed-format, isCodexUpgraded predicate, smartEncrypt schema-version dispatch.
  - **UI v0.29.8** rewires everything:
    - Deleted: `src/lib/encryptor.ts`, `src/lib/encryptorV2.ts`, `src/lib/__tests__/encryption.test.ts`, `src/kadena/wallet/WalletStorage.ts` (renamed).
    - New: `src/lib/smart-encrypt-browser.ts` — ~5-line wrapper that reads `localStorage.codex_schema_version` and delegates to core's pure `smartEncrypt`. Browser-specific boundary stays at the UI; core stays portable.
    - Renamed: `WalletStorage` → `LocalStorageCodexAdapter` (also drops its local `SEED_TYPE_MIGRATION` table in favour of core's). All 6 importers updated (wallet-context, auth-context, ChangePasswordModal, CreateOuroAccount, CreateKadenaSeed, CodexSettingsTab).
    - 13 import sites across 9 files re-pointed to `@stoachain/ouronet-core/crypto` or `@/lib/smart-encrypt-browser` (the 3 smartEncrypt callers).
- **Known outstanding:**
  - **Phase 5** (next) — **the "Ploi goes green" commit.** Publish `@stoachain/ouronet-core@1.0.0` (or `0.10.0`) to GitHub Packages, swap OuronetUI's `package.json` dep from `file:../OuronetCore` to `^0.10.0`, add `.npmrc` pointing at GitHub Packages with `NPM_TOKEN` auth, bump UI to (probably) `v0.30.0` as a symbolic milestone. Once this lands, `npm ci` works everywhere — Ploi dev/main deploys recover, HUB can start pulling core, team laptops can clone-and-run.
  - **Phase 6** (final) — docs cleanup: CLAUDE.md "Kadena Integration" stale paths, CFM_BUILD_GUIDE references, cross-link OuronetCore/HUB_HANDOFF READMEs. Small.
- **User directives in play:**
  - Flow A confirmed (develop with `file:../OuronetCore`, publish at the end). Ploi dev/main deploys intentionally red since Phase 2c — this ends with Phase 5.
  - Smoke-testing is passive now: user tested a random sample of on-chain transactions (unwrap wSTOA, cross-chain aggregate) and both worked after Phase 3b.2. Firestarter un-tested but structurally equivalent. "If problems arise clients will tell us, we can fix as we go."
- **Drift notes:** none. Every phase shipped with local `npm run validate` green + core tests green (193 pass). No behavioural drift introduced in Phase 4 — purely structural moves + one rename.
