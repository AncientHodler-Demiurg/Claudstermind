# Log — OuronetUI

> One paragraph per meaningful session. Newest at the top. Not a commit log (that's `src/constants/changelog.ts`) — this is the higher-level "what happened in this session and what did we learn".

## 2026-04-23 — Core npmjs migration + DALOS Crypto genesis audit + full TS port plan

Multi-day session spanning (1) completing the OuronetCore npmjs migration so Ploi can auto-deploy without auth tokens — bumped core v1.1.0 → v1.2.2 after three publish-workflow attempts (ENEEDAUTH due to `setup-node` scope quirk + wrong secret name `NPM_TOKEN` vs actual `NPMPUSHER`), UI v0.30.3 → v0.30.4, fresh install confirmed working, Ploi deploy verified via Last-Modified header change; (2) a **massive new initiative**: the DALOS Cryptography TypeScript port, to eliminate `go.ouronetwork.io/api/generate` as a dependency; this started with a complete audit of the Go reference at `D:/_Claude/DALOS_Crypto/` (not yet in the Claudstermind cluster) covering every file (`Parameters.go`, `PointConverter.go`, `PointOperations.go`, `KeyGeneration.go`, `Schnorr.go`, `Dalos.go`, plus the newly-inlined `Blake3/*.go` and `AES.go`), independent mathematical verification of the curve parameters via both Python (gmpy2) and Sage (all 7 tests pass under 1 second — P prime, Q prime, cofactor R=4, d=-26 is a QNR, G on curve, [Q]·G = O, safe-scalar bounds), generating an 85-vector reproducible test-vector corpus (50 bitstring + 15 seed-words + 20 Schnorr sign/verify, 20/20 self-verify), proving determinism via regeneration diff (only timestamp + 20 Schnorr sigs vary per run, 64 deterministic records byte-identical); (3) five commits landed on `StoaChain/DALOS_Crypto` main with tags v1.0.0 (Genesis freeze), v1.1.0 (LICENSE + Blake3/AES inlined + 85 vectors + TS port plan), v1.1.1 (validation log), v1.1.2 (author credit Kjrekntolopon@ancientholdings.eu + FUTURE.md with PQ research direction), v1.1.3 (comprehensive per-phase plan v2); (4) lots of design decisions locked: 40×40 bitmap (black=1, row-major TTB-LTR, strict B/W) becomes the 6th key-gen input type, AES stays as-is (doesn't affect account strings), Schnorr gets 7 Category-B hardening items in the TS port only, Genesis-address freeze is permanent with any change becoming Gen-2, post-quantum direction is NEW primitive families NOT bigger curves, the 3-layer npm architecture (dalos-blake3 → dalos-crypto → ouronet-core) is adopted; (5) author credit updated across LICENSE + README + AUDIT.md naming Kjrekntolopon (Geschäftsführer of AncientHoldings GmbH) as the DALOS author; (6) operator paused before starting Phase 0a (bitmap input to Go reference) to trigger `::cmsync` and force continuous-write-back catch-up — this LOG entry is part of that catch-up. **Session continues.** Next `Exec: begin Phase 0a` starts 11-14 weeks of TS porting work spanning `StoaChain/Blake3`, `StoaChain/DALOS_Crypto`, `StoaChain/OuronetCore`, and finally `StoaChain/OuronetUI`.

## 2026-04-22 — Project added to Claudstermind; Phase 1 of core extraction landed

Long multi-day session. Completed `docs/EXTRACT_OURONET_CORE_PLAN.md` phases -1.1 through 1 inclusive. Highlights: (1) bootstrapped GitHub Actions CI for OuronetUI for the first time in its life; (2) pruned 14 silently-red ICO tests and wired a working `npm test` pipeline; (3) wrote 227 regression tests across encryption, guard analysis, signing primitives, gas math, node failover, and Pact code templates — the "safety net" for the extraction; (4) three in-place portability refactors (`window.crypto` → `globalThis.crypto` etc.); (5) scaffolded the sibling `StoaChain/OuronetCore` repo and moved the first five pure modules into `@stoachain/ouronet-core@0.2.0`; (6) rewrote 164 import sites across the UI repo; (7) burned three CI follow-up commits (v0.29.1a→b→c) to unblock cross-org checkout, fix the secret name, and correct the install order; (8) established a secure git-push mechanism via a gitignored PAT file and git's credential-helper-inline pattern; (9) scaffolded this Claudstermind entry at session close. Both CIs green. 117 tests in UI + 110 in core = 227 total. Owner's smoke test of Phase 1 runtime behaviour is pending.
