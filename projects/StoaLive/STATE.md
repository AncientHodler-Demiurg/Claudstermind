# State — StoaLive

> Current-state snapshot. Updated at every session close. Under 15 lines total.

- **Version at close:** no versioning yet (spec-only; no `package.json` / `lib/version.ts`)
- **Open plan:** Phase 0 — Discovery (stack + integration path selection). No `plans/` file exists; `IMPLEMENTATION_ROADMAP.md` is the phased plan.
- **Last session (2026-04-22):** project added to Claudstermind. `CLAUDE.md` written at repo root summarising hard constraints + derived-metric formulas. No source code authored.
- **Known outstanding:**
  - Phase 0 decisions not made: backend language/framework, frontend framework, 3D library (Three.js / PixiJS / raw WebGL?), indexer-vs-direct-node-API data-access path
  - `README.md` references `AI_BUILD_BRIEF.md` which does not exist in the repo (handoff lives in `IMPLEMENTATION_ROADMAP.md` instead)
  - `CLAUDE.md` is currently untracked (created this session, not yet committed)
- **Drift notes:** none. Repo is exactly one commit (`262052b`) plus the untracked `CLAUDE.md`.
